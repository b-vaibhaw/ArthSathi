# ArthaSathi LLM — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core PyTorch ────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ── HuggingFace Ecosystem ───────────────────────────────────
transformers>=4.35.0
tokenizers>=0.15.0
datasets>=2.14.0
accelerate>=0.24.0
peft>=0.6.0                          # LoRA fine-tuning

# ── Quantization ────────────────────────────────────────────
bitsandbytes>=0.41.0                 # INT8 / 4-bit quantization

# ── RAG / Vector Search ─────────────────────────────────────
faiss-cpu>=1.7.4                     # Use faiss-gpu if NVIDIA GPU
sentence-transformers>=2.2.2         # Multilingual embeddings

# ── API Server ───────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
httpx>=0.25.0
python-multipart>=0.0.6              # Form data handling

# ── Voice / Audio ────────────────────────────────────────────
openai-whisper>=20231117             # Speech-to-text
gtts>=2.4.0                          # Google Text-to-Speech
librosa>=0.10.0                      # Audio processing
soundfile>=0.12.0
ffmpeg-python>=0.2.0

# ── SMS ─────────────────────────────────────────────────────
twilio>=8.10.0                       # SMS sending

# ── Data Processing ─────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
ftfy>=6.1.1                          # Unicode text fixing
langdetect>=1.0.9                    # Language detection
PyMuPDF>=1.23.0                      # PDF extraction (RBI/SEBI docs)
beautifulsoup4>=4.12.0               # HTML parsing
lxml>=4.9.0

# ── ML Evaluation ───────────────────────────────────────────
sacrebleu>=2.3.1                     # BLEU score
jiwer>=3.0.3                         # WER for ASR evaluation
evaluate>=0.4.1                      # HuggingFace evaluation library

# ── Utilities ────────────────────────────────────────────────
tqdm>=4.66.0
rich>=13.6.0                         # Pretty terminal output
python-dotenv>=1.0.0                 # .env file loading
watchfiles>=0.21.0                   # File watching for dev server
